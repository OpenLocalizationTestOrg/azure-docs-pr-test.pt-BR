---
title: Failover de teste (VMM para VMM) no Azure Site Recovery | Microsoft Docs
description: "O Azure Site Recovery coordena a replicação, o failover e a recuperação de máquinas virtuais e servidores físicos. Saiba mais sobre o failover no Azure ou em um datacenter secundário."
services: site-recovery
documentationcenter: 
author: prateek9us
manager: gauravd
editor: 
ms.assetid: 44813a48-c680-4581-a92e-cecc57cc3b1e
ms.service: site-recovery
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: storage-backup-recovery
ms.date: 06/05/2017
ms.author: pratshar
ms.openlocfilehash: afc4790d5714ce7145c8f4291a05acc2e9882a9b
ms.sourcegitcommit: f537befafb079256fba0529ee554c034d73f36b0
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/11/2017
---
# <a name="test-failover-vmm-to-vmm-in-site-recovery"></a><span data-ttu-id="dfd41-104">Failover de teste (VMM para VMM) no Site Recovery</span><span class="sxs-lookup"><span data-stu-id="dfd41-104">Test failover (VMM to VMM) in Site Recovery</span></span>


<span data-ttu-id="dfd41-105">Este artigo fornece informações e instruções sobre como fazer um failover de teste ou uma simulação de DR (recuperação de desastre) de VMs (máquinas virtuais) e servidores físicos protegidos com o Azure Site Recovery.</span><span class="sxs-lookup"><span data-stu-id="dfd41-105">This article provides information and instructions for doing a test failover or a disaster recovery (DR) drill of virtual machines (VMs) and physical servers that are protected with Azure Site Recovery.</span></span> <span data-ttu-id="dfd41-106">Você usará um site local gerenciado pelo System Center Virtual Machine Manager (VMM) como o site de recuperação.</span><span class="sxs-lookup"><span data-stu-id="dfd41-106">You'll use a System Center Virtual Machine Manager (VMM)-managed on-premises site as the recovery site.</span></span>

<span data-ttu-id="dfd41-107">Execute um failover de teste para validar sua estratégia de replicação ou realize uma simulação de DR sem nenhuma perda de dados ou tempo de inatividade.</span><span class="sxs-lookup"><span data-stu-id="dfd41-107">You run a test failover to validate your replication strategy or perform a DR drill without any data loss or downtime.</span></span> <span data-ttu-id="dfd41-108">Um failover de teste não afeta a replicação em andamento nem o ambiente de produção.</span><span class="sxs-lookup"><span data-stu-id="dfd41-108">A test failover doesn't have any impact on the ongoing replication or on your production environment.</span></span> <span data-ttu-id="dfd41-109">Execute-o em uma máquina virtual ou um [plano de recuperação](site-recovery-create-recovery-plans.md).</span><span class="sxs-lookup"><span data-stu-id="dfd41-109">You can run it on either a virtual machine or a [recovery plan](site-recovery-create-recovery-plans.md).</span></span> <span data-ttu-id="dfd41-110">Quando estiver disparando um failover de teste, você precisa especificar a rede à qual as máquinas virtuais de teste se conectarão.</span><span class="sxs-lookup"><span data-stu-id="dfd41-110">When you're triggering a test failover, you need to specify the network that the test virtual machines will connect to.</span></span> <span data-ttu-id="dfd41-111">Acompanhe o progresso do failover de teste na página **Trabalhos**.</span><span class="sxs-lookup"><span data-stu-id="dfd41-111">You can track the progress of the test failover on the **Jobs** page.</span></span>  

<span data-ttu-id="dfd41-112">Em caso de comentários ou perguntas, poste-os ao final deste artigo ou no [Fórum dos Serviços de Recuperação do Azure](https://social.msdn.microsoft.com/forums/azure/home?forum=hypervrecovmgr).</span><span class="sxs-lookup"><span data-stu-id="dfd41-112">If you have any comments or questions, post them at the bottom of this article or on the [Azure Recovery Services Forum](https://social.msdn.microsoft.com/forums/azure/home?forum=hypervrecovmgr).</span></span>


## <a name="prepare-the-infrastructure-for-test-failover"></a><span data-ttu-id="dfd41-113">Preparar a infraestrutura para o failover de teste</span><span class="sxs-lookup"><span data-stu-id="dfd41-113">Prepare the infrastructure for test failover</span></span>
<span data-ttu-id="dfd41-114">Se desejar executar um failover de teste usando uma rede existente, prepare o Active Directory, o DHCP e o DNS nessa rede.</span><span class="sxs-lookup"><span data-stu-id="dfd41-114">If you want to run a test failover by using an existing network, prepare Active Directory, DHCP, and DNS in that network.</span></span>

<span data-ttu-id="dfd41-115">Se desejar executar um failover de teste usando a opção para criar redes VM automaticamente, adicione uma etapa manual antes do Grupo 1 no plano de recuperação que você pretende usar para o failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-115">If you want to run a test failover by using the option to create VM networks automatically, add a manual step before Group-1 in the recovery plan that you’re going to use for the test failover.</span></span> <span data-ttu-id="dfd41-116">Em seguida, adicione os recursos de infraestrutura à rede criada automaticamente antes de executar o failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-116">Then, add the infrastructure resources to the automatically created network before you run the test failover.</span></span>

### <a name="things-to-note"></a><span data-ttu-id="dfd41-117">Elementos a serem observados</span><span class="sxs-lookup"><span data-stu-id="dfd41-117">Things to note</span></span>
<span data-ttu-id="dfd41-118">Quando estiver replicando para um site secundário, o tipo de rede usado pelo computador de réplica não precisa corresponder ao tipo de rede lógica usado para o failover de teste, mas algumas combinações podem não funcionar.</span><span class="sxs-lookup"><span data-stu-id="dfd41-118">When you're replicating to a secondary site, the type of network that the replica machine uses doesn’t need to match the type of logical network used for test failover, but some combinations might not work.</span></span> <span data-ttu-id="dfd41-119">Quando a réplica usa isolamento baseado em VLAN e DHCP, a rede VM da réplica não precisa de um pool de endereços IP estáticos.</span><span class="sxs-lookup"><span data-stu-id="dfd41-119">If the replica uses DHCP and VLAN-based isolation, the VM network for the replica doesn't need a static IP address pool.</span></span> <span data-ttu-id="dfd41-120">Portanto, o uso da Virtualização de Rede do Windows para o failover de teste não funcionará porque nenhum pool de endereços está disponível.</span><span class="sxs-lookup"><span data-stu-id="dfd41-120">So using Windows Network Virtualization for the test failover won't work because no address pools are available.</span></span> 

<span data-ttu-id="dfd41-121">Além disso, o failover de teste não funcionará se a rede de réplica não usar nenhum isolamento e a rede de teste usar a Virtualização de Rede do Windows.</span><span class="sxs-lookup"><span data-stu-id="dfd41-121">In addition, the test failover won't work if the replica network uses no isolation and the test network uses Windows Network Virtualization.</span></span> <span data-ttu-id="dfd41-122">Isso ocorre porque a rede sem isolamento não tem as sub-redes necessárias para criar uma rede da Virtualização de Rede do Windows.</span><span class="sxs-lookup"><span data-stu-id="dfd41-122">This is because the no-isolation network doesn't have the subnets required to create a Windows Network Virtualization network.</span></span>

<span data-ttu-id="dfd41-123">O modo como as máquinas virtuais de réplica são conectadas às redes VM mapeadas após o failover depende de como a rede VM é configurada no console do VMM.</span><span class="sxs-lookup"><span data-stu-id="dfd41-123">How replica virtual machines are connected to mapped VM networks after failover depends on how the VM network is configured in the VMM console.</span></span>

#### <a name="vm-network-configured-with-no-isolation-or-vlan-isolation"></a><span data-ttu-id="dfd41-124">Rede VM configurada sem isolamento ou com isolamento de VLAN</span><span class="sxs-lookup"><span data-stu-id="dfd41-124">VM network configured with no isolation or VLAN isolation</span></span>
<span data-ttu-id="dfd41-125">Se o DHCP for definido para a rede VM, a máquina virtual de réplica será conectada à ID da VLAN usando as configurações especificadas para o site da rede na rede lógica associada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-125">If DHCP is defined for the VM network, the replica virtual machine is connected to the VLAN ID through the settings that are specified for the network site in the associated logical network.</span></span> <span data-ttu-id="dfd41-126">A máquina virtual recebe seu endereço IP do servidor DHCP disponível.</span><span class="sxs-lookup"><span data-stu-id="dfd41-126">The virtual machine receives its IP address from the available DHCP server.</span></span> 

<span data-ttu-id="dfd41-127">Não é necessário definir um pool de endereços IP estáticos para a rede VM de destino.</span><span class="sxs-lookup"><span data-stu-id="dfd41-127">You don't need to define a static IP address pool for the target VM network.</span></span> <span data-ttu-id="dfd41-128">Se um pool de endereços IP estáticos for usado para a rede VM, a máquina virtual de réplica será conectada à ID da VLAN por meio das configurações especificadas para o site da rede na rede lógica associada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-128">If a static IP address pool is used for the VM network, the replica virtual machine is connected to the VLAN ID through the settings that are specified for the network site in the associated logical network.</span></span>

<span data-ttu-id="dfd41-129">A máquina virtual receberá seu endereço IP do pool definido para a rede VM.</span><span class="sxs-lookup"><span data-stu-id="dfd41-129">The virtual machine receives its IP address from the pool that's defined for the VM network.</span></span> <span data-ttu-id="dfd41-130">Se um pool de endereços IP estáticos não for definido na rede VM de destino, a alocação do endereço IP falhará.</span><span class="sxs-lookup"><span data-stu-id="dfd41-130">If a static IP address pool isn't defined on the target VM network, IP address allocation will fail.</span></span> <span data-ttu-id="dfd41-131">Crie o pool de endereços IP nos servidores VMM de origem e destino que você usará para proteção e recuperação.</span><span class="sxs-lookup"><span data-stu-id="dfd41-131">Create the IP address pool on both the source and target VMM servers that you will use for protection and recovery.</span></span>

#### <a name="vm-network-with-windows-network-virtualization"></a><span data-ttu-id="dfd41-132">Rede VM com Virtualização de Rede do Windows</span><span class="sxs-lookup"><span data-stu-id="dfd41-132">VM network with Windows Network Virtualization</span></span>
<span data-ttu-id="dfd41-133">Se uma rede VM for configurada com a Virtualização de Rede do Windows, você deverá definir um pool estático para a rede VM de destino, independentemente de a rede VM de origem estar configurada para usar o DHCP ou um pool de endereços IP estáticos.</span><span class="sxs-lookup"><span data-stu-id="dfd41-133">If a VM network is configured with Windows Network Virtualization, you should define a static pool for the target VM network, regardless of whether the source VM network is configured to use DHCP or a static IP address pool.</span></span> 

<span data-ttu-id="dfd41-134">Se você definir o DHCP, o servidor VMM de destino atuará como um servidor DHCP e fornecerá um endereço IP do pool definido para a rede VM de destino.</span><span class="sxs-lookup"><span data-stu-id="dfd41-134">If you define DHCP, the target VMM server acts as a DHCP server and provides an IP address from the pool that's defined for the target VM network.</span></span> <span data-ttu-id="dfd41-135">Se o uso de um pool de endereços IP estáticos for definido para o servidor de origem, o servidor VMM de destino alocará um endereço IP do pool.</span><span class="sxs-lookup"><span data-stu-id="dfd41-135">If use of a static IP address pool is defined for the source server, the target VMM server allocates an IP address from the pool.</span></span> <span data-ttu-id="dfd41-136">Em ambos os casos, a alocação de endereço IP falhará se um pool de endereços IP estáticos não for definido.</span><span class="sxs-lookup"><span data-stu-id="dfd41-136">In both cases, IP address allocation will fail if a static IP address pool is not defined.</span></span>


### <a name="prepare-dhcp"></a><span data-ttu-id="dfd41-137">Preparar o DHCP</span><span class="sxs-lookup"><span data-stu-id="dfd41-137">Prepare DHCP</span></span>
<span data-ttu-id="dfd41-138">Se as máquinas virtuais envolvidas no failover de teste usarem o DHCP, crie um servidor DHCP de teste na rede isolada que é criada para fins de failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-138">If the virtual machines involved in test failover use DHCP, create a test DHCP server within the isolated network for the purpose of test failover.</span></span>

### <a name="prepare-active-directory"></a><span data-ttu-id="dfd41-139">Preparar o Active Directory</span><span class="sxs-lookup"><span data-stu-id="dfd41-139">Prepare Active Directory</span></span>
<span data-ttu-id="dfd41-140">Para executar um failover de teste em um teste de aplicativo, você precisará de uma cópia do ambiente de produção do Active Directory no ambiente de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-140">To run a test failover for application testing, you need a copy of the production Active Directory environment in your test environment.</span></span> <span data-ttu-id="dfd41-141">Para obter mais informações, examine as [considerações sobre failover de teste para o Active Directory](site-recovery-active-directory.md#test-failover-considerations).</span><span class="sxs-lookup"><span data-stu-id="dfd41-141">For more information, review the [test failover considerations for Active Directory](site-recovery-active-directory.md#test-failover-considerations).</span></span>

### <a name="prepare-dns"></a><span data-ttu-id="dfd41-142">Preparar o DNS</span><span class="sxs-lookup"><span data-stu-id="dfd41-142">Prepare DNS</span></span>
<span data-ttu-id="dfd41-143">Prepare um servidor DNS para o failover de teste da seguinte forma:</span><span class="sxs-lookup"><span data-stu-id="dfd41-143">Prepare a DNS server for the test failover as follows:</span></span>

* <span data-ttu-id="dfd41-144">**DHCP**: se as máquinas virtuais usarem o DHCP, o endereço IP do DNS de teste deverá ser atualizado no servidor DHCP de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-144">**DHCP**: If virtual machines use DHCP, the IP address of the test DNS should be updated on the test DHCP server.</span></span> <span data-ttu-id="dfd41-145">Quando você usa um tipo de rede da Virtualização de Rede do Windows, o servidor VMM atua como o servidor DHCP.</span><span class="sxs-lookup"><span data-stu-id="dfd41-145">If you’re using a network type of Windows Network Virtualization, the VMM server acts as the DHCP server.</span></span> <span data-ttu-id="dfd41-146">Portanto, o endereço IP do DNS deve ser atualizado na rede de failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-146">Therefore, the IP address of DNS should be updated in the test failover network.</span></span> <span data-ttu-id="dfd41-147">Nesse caso, as máquinas virtuais se registrarão no servidor DNS relevante.</span><span class="sxs-lookup"><span data-stu-id="dfd41-147">In this case, the virtual machines register themselves to the relevant DNS server.</span></span>
* <span data-ttu-id="dfd41-148">**Endereço estático**: se as máquinas virtuais usarem um endereço IP estático, o endereço IP do servidor DNS de teste deverá ser atualizado na rede de failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-148">**Static address**: If virtual machines use a static IP address, the IP address of the test DNS server should be updated in test failover network.</span></span> <span data-ttu-id="dfd41-149">Talvez você precise atualizar o DNS com o endereço IP das máquinas virtuais de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-149">You might need to update DNS with the IP address of the test virtual machines.</span></span> <span data-ttu-id="dfd41-150">É possível usar o seguinte script de exemplo para este fim:</span><span class="sxs-lookup"><span data-stu-id="dfd41-150">You can use the following sample script for this purpose:</span></span>

        Param(
        [string]$Zone,
        [string]$name,
        [string]$IP
        )
        $Record = Get-DnsServerResourceRecord -ZoneName $zone -Name $name
        $newrecord = $record.clone()
        $newrecord.RecordData[0].IPv4Address  =  $IP
        Set-DnsServerResourceRecord -zonename $zone -OldInputObject $record -NewInputObject $Newrecord



## <a name="run-a-test-failover"></a><span data-ttu-id="dfd41-151">Execute um teste de failover</span><span class="sxs-lookup"><span data-stu-id="dfd41-151">Run a test failover</span></span>
<span data-ttu-id="dfd41-152">Este procedimento descreve como executar um failover de teste para um plano de recuperação.</span><span class="sxs-lookup"><span data-stu-id="dfd41-152">This procedure describes how to run a test failover for a recovery plan.</span></span> <span data-ttu-id="dfd41-153">Como alternativa, você pode executar o failover de uma máquina virtual individual na guia **Máquinas Virtuais**.</span><span class="sxs-lookup"><span data-stu-id="dfd41-153">Alternatively, you can run the failover for a single virtual machine on the **Virtual Machines** tab.</span></span>

![Folha Failover de teste](./media/site-recovery-test-failover-vmm-to-vmm/TestFailover.png)

1. <span data-ttu-id="dfd41-155">Selecione **Planos de Recuperação** > *recoveryplan_name*.</span><span class="sxs-lookup"><span data-stu-id="dfd41-155">Select **Recovery Plans** > *recoveryplan_name*.</span></span> <span data-ttu-id="dfd41-156">Clique em **Failover** > **Test Failover**.</span><span class="sxs-lookup"><span data-stu-id="dfd41-156">Click **Failover** > **Test Failover**.</span></span>
1. <span data-ttu-id="dfd41-157">Na folha **Failover de Teste** , especifique como as máquinas virtuais devem ser conectadas às redes após o failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-157">On the **Test Failover** blade, specify how virtual machines should be connected to networks after the test failover.</span></span> <span data-ttu-id="dfd41-158">Para obter mais informações, consulte as [opções de rede](#network-options-in-site-recovery).</span><span class="sxs-lookup"><span data-stu-id="dfd41-158">For more information, see the [network options](#network-options-in-site-recovery).</span></span>
1. <span data-ttu-id="dfd41-159">Acompanhe o progresso do failover na guia **Trabalhos** .</span><span class="sxs-lookup"><span data-stu-id="dfd41-159">Track failover progress on the **Jobs** tab.</span></span>
1. <span data-ttu-id="dfd41-160">Depois que o failover for concluído, verifique se as máquinas virtuais são iniciadas com êxito.</span><span class="sxs-lookup"><span data-stu-id="dfd41-160">After failover is complete, verify that the virtual machines start successfully.</span></span>
1. <span data-ttu-id="dfd41-161">Quando terminar, clique em **Failover de teste de limpeza** no plano de recuperação.</span><span class="sxs-lookup"><span data-stu-id="dfd41-161">When you're done, click **Cleanup test failover** on the recovery plan.</span></span> <span data-ttu-id="dfd41-162">Em **Observações**, registre e salve todas as observações associadas ao failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-162">In **Notes**, record and save any observations associated with the test failover.</span></span> <span data-ttu-id="dfd41-163">Essa etapa exclui as máquinas virtuais e redes virtuais que foram criadas durante o failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-163">This step deletes the virtual machines and networks that were created during test failover.</span></span>


## <a name="network-options-in-site-recovery"></a><span data-ttu-id="dfd41-164">Opções de rede na recuperação de site</span><span class="sxs-lookup"><span data-stu-id="dfd41-164">Network options in Site Recovery</span></span>

<span data-ttu-id="dfd41-165">Ao executar um failover de teste, você deverá selecionar as configurações de rede para os computadores de réplica de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-165">When you run a test failover, you're asked to select network settings for test replica machines.</span></span> <span data-ttu-id="dfd41-166">Você tem várias opções.</span><span class="sxs-lookup"><span data-stu-id="dfd41-166">You have multiple options.</span></span>  

| <span data-ttu-id="dfd41-167">**Opção de failover de teste**</span><span class="sxs-lookup"><span data-stu-id="dfd41-167">**Test failover option**</span></span> | <span data-ttu-id="dfd41-168">**Descrição**</span><span class="sxs-lookup"><span data-stu-id="dfd41-168">**Description**</span></span> | <span data-ttu-id="dfd41-169">**Verificação do failover**</span><span class="sxs-lookup"><span data-stu-id="dfd41-169">**Failover check**</span></span> | <span data-ttu-id="dfd41-170">**Detalhes**</span><span class="sxs-lookup"><span data-stu-id="dfd41-170">**Details**</span></span> |
| --- | --- | --- | --- |
| <span data-ttu-id="dfd41-171">**Fazer failover em um site secundário do VMM – sem rede**</span><span class="sxs-lookup"><span data-stu-id="dfd41-171">**Fail over to a secondary VMM site--without network**</span></span> |<span data-ttu-id="dfd41-172">Não selecione uma rede VM.</span><span class="sxs-lookup"><span data-stu-id="dfd41-172">Don't select a VM network.</span></span> |<span data-ttu-id="dfd41-173">O failover verifica se os computadores de teste são criados.</span><span class="sxs-lookup"><span data-stu-id="dfd41-173">Failover checks that test machines are created.</span></span><br/><br/><span data-ttu-id="dfd41-174">A máquina virtual de teste é criada no host em que a máquina virtual de réplica está localizada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-174">The test virtual machine is created on the host where the replica virtual machine exists.</span></span> <span data-ttu-id="dfd41-175">Ela não é adicionada à nuvem na qual a máquina virtual de réplica está localizada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-175">It isn’t added to the cloud where the replica virtual machine is located.</span></span> |<p><span data-ttu-id="dfd41-176">O computador com failover não é conectado a nenhuma rede.</span><span class="sxs-lookup"><span data-stu-id="dfd41-176">The failed-over machine isn’t connected to any network.</span></span><br/><br/><span data-ttu-id="dfd41-177">O computador poderá ser conectado a uma rede VM depois que ela for criada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-177">The machine can be connected to a VM network after it has been created.</span></span> |
| <span data-ttu-id="dfd41-178">**Fazer failover em um site secundário do VMM – com rede**</span><span class="sxs-lookup"><span data-stu-id="dfd41-178">**Fail over to a secondary VMM site--with network**</span></span> |<span data-ttu-id="dfd41-179">Selecione uma rede VM existente.</span><span class="sxs-lookup"><span data-stu-id="dfd41-179">Select an existing VM network.</span></span> |<span data-ttu-id="dfd41-180">O failover verifica se as máquinas virtuais foram criadas.</span><span class="sxs-lookup"><span data-stu-id="dfd41-180">Failover checks that virtual machines are created.</span></span> |<span data-ttu-id="dfd41-181">A máquina virtual de teste é criada no host em que a máquina virtual de réplica está localizada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-181">The test virtual machine is created on the host where the replica virtual machine exists.</span></span> <span data-ttu-id="dfd41-182">Ela não é adicionada à nuvem na qual a máquina virtual de réplica está localizada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-182">It isn’t added to the cloud where the replica virtual machine is located.</span></span><br/><br/><span data-ttu-id="dfd41-183">Crie uma rede VM isolada da rede de produção.</span><span class="sxs-lookup"><span data-stu-id="dfd41-183">Create a VM network that's isolated from your production network.</span></span><br/><br/><span data-ttu-id="dfd41-184">Se você estiver usando uma rede baseada em VLAN, recomendamos criar uma rede lógica separada (não usada em produção) no VMM para essa finalidade.</span><span class="sxs-lookup"><span data-stu-id="dfd41-184">If you're using a VLAN-based network, we recommend that you create a separate logical network (not used in production) in VMM for this purpose.</span></span> <span data-ttu-id="dfd41-185">Essa rede lógica é usada para criar redes VM para failovers de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-185">This logical network is used to create VM networks for test failovers.</span></span><br/><br/><span data-ttu-id="dfd41-186">A rede lógica deve estar associada a, pelo menos, um dos adaptadores de rede de todos os servidores do Hyper-V que hospedam máquinas virtuais.</span><span class="sxs-lookup"><span data-stu-id="dfd41-186">The logical network should be associated with at least one of the network adapters of all the Hyper-V servers that are hosting virtual machines.</span></span><br/><br/><span data-ttu-id="dfd41-187">Para redes lógicas VLAN, os sites de rede adicionados à rede lógica devem ser isolados.</span><span class="sxs-lookup"><span data-stu-id="dfd41-187">For VLAN logical networks, the network sites that you add to the logical network should be isolated.</span></span><br/><br/><span data-ttu-id="dfd41-188">Se você está estiver usando uma rede lógica baseada na Virtualização de Rede do Windows, o Azure Site Recovery criará automaticamente redes de VM isoladas.</span><span class="sxs-lookup"><span data-stu-id="dfd41-188">If you’re using a Windows Network Virtualization–based logical network, Azure Site Recovery automatically creates isolated VM networks.</span></span> |
| <span data-ttu-id="dfd41-189">**Fazer failover em um site secundário do VMM – criar uma rede**</span><span class="sxs-lookup"><span data-stu-id="dfd41-189">**Fail over to a secondary VMM site--create a network**</span></span> |<span data-ttu-id="dfd41-190">Uma rede de teste temporária é criada automaticamente com base na configuração especificada em **Rede Lógica** e seus sites de rede relacionados.</span><span class="sxs-lookup"><span data-stu-id="dfd41-190">A temporary test network is created automatically based on the setting that you specify in **Logical Network** and its related network sites.</span></span> |<span data-ttu-id="dfd41-191">O failover verifica se as máquinas virtuais foram criadas.</span><span class="sxs-lookup"><span data-stu-id="dfd41-191">Failover checks that virtual machines are created.</span></span> |<span data-ttu-id="dfd41-192">Use esta opção se o plano de recuperação usa mais de uma rede VM.</span><span class="sxs-lookup"><span data-stu-id="dfd41-192">Use this option if the recovery plan uses more than one VM network.</span></span> <span data-ttu-id="dfd41-193">Caso esteja usando redes da Virtualização de Rede do Windows, essa opção pode ser usada para criar automaticamente redes VM com as mesmas configurações (sub-redes e pools de endereços IP) na rede da máquina virtual de réplica.</span><span class="sxs-lookup"><span data-stu-id="dfd41-193">If you're using Windows Network Virtualization networks, this option can automatically create VM networks with the same settings (subnets and IP address pools) in the network of the replica virtual machine.</span></span> <span data-ttu-id="dfd41-194">Essas redes VM são removidas automaticamente depois que o failover de teste é concluído.</span><span class="sxs-lookup"><span data-stu-id="dfd41-194">These VM networks are cleaned up automatically after the test failover is complete.</span></span></p><p><span data-ttu-id="dfd41-195">A máquina virtual de teste é criada no host em que a máquina virtual de réplica está localizada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-195">The test virtual machine is created on the host where the replica virtual machine exists.</span></span> <span data-ttu-id="dfd41-196">Ela não é adicionada à nuvem na qual a máquina virtual de réplica está localizada.</span><span class="sxs-lookup"><span data-stu-id="dfd41-196">It isn’t added to the cloud where the replica virtual machine is located.</span></span> |

> [!TIP]
> <span data-ttu-id="dfd41-197">O endereço IP fornecido a uma máquina virtual durante o failover de teste é o mesmo endereço IP que a máquina virtual receberia em um failover planejado ou não planejado (supondo que o endereço IP esteja disponível na rede de failover de teste).</span><span class="sxs-lookup"><span data-stu-id="dfd41-197">The IP address given to a virtual machine during test failover is the same IP address that the virtual machine would receive for a planned or unplanned failover (presuming that the IP address is available in the test failover network).</span></span> <span data-ttu-id="dfd41-198">Se o mesmo endereço IP não estiver disponível na rede de failover de teste, a máquina virtual receberá outro endereço IP que está disponível na rede de failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-198">If the same IP address isn't available in the test failover network, the virtual machine receives another IP address that's available in the test failover network.</span></span>
>
>


## <a name="test-failover-to-a-production-network-on-a-recovery-site"></a><span data-ttu-id="dfd41-199">Failover de teste em uma rede de produção em um site de recuperação</span><span class="sxs-lookup"><span data-stu-id="dfd41-199">Test failover to a production network on a recovery site</span></span>
<span data-ttu-id="dfd41-200">Recomendamos que, ao fazer um failover de teste, você escolha uma rede diferente da rede do site de recuperação de produção fornecida no [mapeamento de rede](https://docs.microsoft.com/azure/site-recovery/site-recovery-network-mapping).</span><span class="sxs-lookup"><span data-stu-id="dfd41-200">We recommend that when you're doing a test failover, you choose a network that is different from your production recovery site network that you provided in [network mapping](https://docs.microsoft.com/azure/site-recovery/site-recovery-network-mapping).</span></span> <span data-ttu-id="dfd41-201">Mas se realmente desejar validar a conectividade de rede de ponta a ponta em uma máquina virtual com failover, observe os seguintes pontos:</span><span class="sxs-lookup"><span data-stu-id="dfd41-201">But if you really want to validate end-to-end network connectivity in a failed-over virtual machine, note the following points:</span></span>

* <span data-ttu-id="dfd41-202">Verifique se a máquina virtual primária está desligada quando estiver fazendo o failover de teste.</span><span class="sxs-lookup"><span data-stu-id="dfd41-202">Make sure that the primary virtual machine is shut down when you're doing the test failover.</span></span> <span data-ttu-id="dfd41-203">Se você não fizer isso, duas máquinas virtuais com a mesma identidade serão executadas na mesma rede ao mesmo tempo.</span><span class="sxs-lookup"><span data-stu-id="dfd41-203">If you don't, two virtual machines with the same identity will be running in the same network at the same time.</span></span> <span data-ttu-id="dfd41-204">Essa situação pode levar a consequências indesejadas.</span><span class="sxs-lookup"><span data-stu-id="dfd41-204">That situation can lead to undesired consequences.</span></span>
* <span data-ttu-id="dfd41-205">As alterações feitas nas máquinas virtuais de failover de teste são perdidas quando elas são limpas.</span><span class="sxs-lookup"><span data-stu-id="dfd41-205">Any changes that you make to the test failover virtual machines are lost when you clean up the test failover virtual machines.</span></span> <span data-ttu-id="dfd41-206">Essas alterações não são replicadas novamente para a máquina virtual primária.</span><span class="sxs-lookup"><span data-stu-id="dfd41-206">These changes are not replicated back to the primary virtual machine.</span></span>
* <span data-ttu-id="dfd41-207">Essa forma de realizar o teste resulta em um tempo de inatividade para o aplicativo de produção.</span><span class="sxs-lookup"><span data-stu-id="dfd41-207">This way of doing testing leads to downtime for your production application.</span></span> <span data-ttu-id="dfd41-208">Solicite aos usuários do aplicativo que não usem o aplicativo quando a simulação de DR estiver em andamento.</span><span class="sxs-lookup"><span data-stu-id="dfd41-208">Ask users of the application not to use the application when the DR drill is in progress.</span></span>  


## <a name="next-steps"></a><span data-ttu-id="dfd41-209">Próximas etapas</span><span class="sxs-lookup"><span data-stu-id="dfd41-209">Next steps</span></span>
<span data-ttu-id="dfd41-210">Depois de executar um failover de teste com êxito, você pode tentar um [failover](site-recovery-failover.md).</span><span class="sxs-lookup"><span data-stu-id="dfd41-210">After you have successfully run a test failover, you can try a [failover](site-recovery-failover.md).</span></span>
