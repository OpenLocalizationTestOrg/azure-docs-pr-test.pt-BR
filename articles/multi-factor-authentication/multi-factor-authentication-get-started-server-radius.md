---
title: "Autenticação RADIUS e Servidor de MFA do Azure | Microsoft Docs"
description: "Esta é a página do Azure Multi-Factor Authentication que auxiliará na implantação de autenticação RADIUS e do servidor Azure Multi-Factor Authentication."
services: multi-factor-authentication
documentationcenter: 
author: kgremban
manager: femila
ms.assetid: f4ba0fb2-2be9-477e-9bea-04c7340c8bce
ms.service: multi-factor-authentication
ms.workload: identity
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: get-started-article
ms.date: 02/26/2017
ms.author: kgremban
ms.reviewer: yossib
ms.custom: H1Hack27Feb2017, it-pro
ms.openlocfilehash: a4c52cc40b17902d92f7a94028ddb3c641911e8d
ms.sourcegitcommit: f537befafb079256fba0529ee554c034d73f36b0
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/11/2017
---
# <a name="integrate-radius-authentication-with-azure-multi-factor-authentication-server"></a><span data-ttu-id="360e3-103">Integrar a autenticação RADIUS com o Servidor de Autenticação Multifator do Azure</span><span class="sxs-lookup"><span data-stu-id="360e3-103">Integrate RADIUS authentication with Azure Multi-Factor Authentication Server</span></span>
<span data-ttu-id="360e3-104">Use a seção de Autenticação RADIUS do Servidor de MFA do Azure para habilitar e configurar a autenticação RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-104">Use the RADIUS Authentication section of Azure MFA Server to enable and configure RADIUS authentication.</span></span> <span data-ttu-id="360e3-105">RADIUS é um protocolo padrão para aceitar solicitações de autenticação e processar essas solicitações.</span><span class="sxs-lookup"><span data-stu-id="360e3-105">RADIUS is a standard protocol to accept authentication requests and to process those requests.</span></span> <span data-ttu-id="360e3-106">O Servidor de Autenticação Multifator do Azure atua como um servidor RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-106">The Azure Multi-Factor Authentication Server acts as a RADIUS server.</span></span> <span data-ttu-id="360e3-107">Insira-o entre o cliente RADIUS (dispositivo VPN) e o destino de autenticação, que pode ser o Active Directory (AD), um diretório LDAP ou outro servidor RADIUS, para adicionar a Autenticação Multifator do Azure.</span><span class="sxs-lookup"><span data-stu-id="360e3-107">Insert it between your RADIUS client (VPN appliance) and your authentication target, which could be Active Directory (AD), an LDAP directory, or another RADIUS server to add Azure Multi-Factor Authentication.</span></span> <span data-ttu-id="360e3-108">Para a MFA (Autenticação Multifator) do Azure funcionar, você deve configurar o Servidor de MFA do Azure para que ele possa se comunicar com os servidores clientes e com o destino de autenticação.</span><span class="sxs-lookup"><span data-stu-id="360e3-108">For Azure Multi-Factor Authentication (MFA) to function, you must configure the Azure MFA Server so that it can communicate with both the client servers and the authentication target.</span></span> <span data-ttu-id="360e3-109">O Servidor de MFA do Azure aceita solicitações de um cliente RADIUS, valida as credenciais no destino de autenticação, adiciona a Autenticação Multifator do Azure e devolve uma resposta ao cliente RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-109">The Azure MFA Server accepts requests from a RADIUS client, validates credentials against the authentication target, adds Azure Multi-Factor Authentication, and sends a response back to the RADIUS client.</span></span> <span data-ttu-id="360e3-110">A solicitação de autenticação só terá êxito se a autenticação primária e a Autenticação Multifator do Azure tiverem êxito.</span><span class="sxs-lookup"><span data-stu-id="360e3-110">The authentication request only succeeds if both the primary authentication and the Azure Multi-Factor Authentication succeed.</span></span>

> [!NOTE]
> <span data-ttu-id="360e3-111">O servidor MFA só dá suporte a PAP (protocolo de autenticação de senha) e o MSCHAPv2 (Microsoft’s Challenge Handshake Authentication Protocol) de protocolos de RADIUS agindo como um servidor RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-111">The MFA Server only supports PAP (password authentication protocol) and MSCHAPv2 (Microsoft's Challenge-Handshake Authentication Protocol) RADIUS protocols when acting as a RADIUS server.</span></span>  <span data-ttu-id="360e3-112">Outros protocolos, como o EAP (Protocolo de Autenticação Extensível) podem ser usados quando o servidor MFA atua como um proxy RADIUS para outro servidor RADIUS que dá suporte a esse protocolo.</span><span class="sxs-lookup"><span data-stu-id="360e3-112">Other protocols, like EAP (extensible authentication protocol), can be used when the MFA server acts as a RADIUS proxy to another RADIUS server that supports that protocol.</span></span>
>
> <span data-ttu-id="360e3-113">Os tokens SMS e OATH unidirecionais não funcionam nessa configuração, pois o servidor MFA não pode iniciar uma resposta de desafio de RADIUS bem-sucedida usando protocolos alternativos.</span><span class="sxs-lookup"><span data-stu-id="360e3-113">In this configuration, one-way SMS and OATH tokens don't work since the MFA Server can't initiate a successful RADIUS Challenge response using alternative protocols.</span></span>

![Autenticação Radius](./media/multi-factor-authentication-get-started-server-rdg/radius.png)

## <a name="add-a-radius-client"></a><span data-ttu-id="360e3-115">Adicionar um cliente RADIUS</span><span class="sxs-lookup"><span data-stu-id="360e3-115">Add a RADIUS client</span></span>
<span data-ttu-id="360e3-116">Para configurar a autenticação RADIUS, instale o servidor Azure Multi-Factor Authentication em um servidor Windows.</span><span class="sxs-lookup"><span data-stu-id="360e3-116">To configure RADIUS authentication, install the Azure Multi-Factor Authentication Server on a Windows server.</span></span> <span data-ttu-id="360e3-117">Se você tiver um ambiente do Active Directory, o servidor deve ser associado ao domínio dentro da rede.</span><span class="sxs-lookup"><span data-stu-id="360e3-117">If you have an Active Directory environment, the server should be joined to the domain inside the network.</span></span> <span data-ttu-id="360e3-118">Use o procedimento a seguir para configurar o servidor Azure Multi-Factor Authentication:</span><span class="sxs-lookup"><span data-stu-id="360e3-118">Use the following procedure to configure the Azure Multi-Factor Authentication Server:</span></span>

1. <span data-ttu-id="360e3-119">No Servidor de Autenticação Multifator do Azure, clique no ícone de Autenticação RADIUS no menu esquerdo.</span><span class="sxs-lookup"><span data-stu-id="360e3-119">In the Azure Multi-Factor Authentication Server, click the RADIUS Authentication icon in the left menu.</span></span>
2. <span data-ttu-id="360e3-120">Marque a caixa de seleção **Habilitar autenticação RADIUS**.</span><span class="sxs-lookup"><span data-stu-id="360e3-120">Check the **Enable RADIUS authentication** checkbox.</span></span>
3. <span data-ttu-id="360e3-121">Na guia Clientes, altere as portas de Autenticação e de Conta se o serviço RADIUS de MFA do Azure precisa escutar solicitações de RADIUS em portas não padrão.</span><span class="sxs-lookup"><span data-stu-id="360e3-121">On the Clients tab, change the Authentication and Accounting ports if the Azure MFA RADIUS service needs to listen for RADIUS requests on non-standard ports.</span></span>
4. <span data-ttu-id="360e3-122">Clique em **Adicionar**.</span><span class="sxs-lookup"><span data-stu-id="360e3-122">Click **Add**.</span></span>
5. <span data-ttu-id="360e3-123">Insira o endereço IP do dispositivo/servidor que fará a autenticação no Servidor de Autenticação Multifator do Azure, um nome de aplicativo (opcional) e um segredo compartilhado.</span><span class="sxs-lookup"><span data-stu-id="360e3-123">Enter the IP address of the appliance/server that will authenticate to the Azure Multi-Factor Authentication Server, an application name (optional), and a shared secret.</span></span>

  <span data-ttu-id="360e3-124">O nome do aplicativo aparece nos relatórios da Autenticação Multifator do Azure e pode ser exibido nas mensagens de autenticação por SMS ou do Aplicativo Móvel.</span><span class="sxs-lookup"><span data-stu-id="360e3-124">The application name appears in Azure Multi-Factor Authentication reports and may be displayed within SMS or Mobile App authentication messages.</span></span>

  <span data-ttu-id="360e3-125">O segredo compartilhado deve ser o mesmo tanto no Servidor de Autenticação Multifator do Azure quanto no dispositivo/servidor.</span><span class="sxs-lookup"><span data-stu-id="360e3-125">The shared secret needs to be the same on both the Azure Multi-Factor Authentication Server and appliance/server.</span></span>

6. <span data-ttu-id="360e3-126">Marque a caixa **Exigir correspondência do usuário da Autenticação Multifator** se todos os usuários foram ou vierem a ser importados para o Servidor e estiverem sujeitos à autenticação multifator.</span><span class="sxs-lookup"><span data-stu-id="360e3-126">Check the **Require Multi-Factor Authentication user match** box if all users have been or will be imported into the Server and subject to multi-factor authentication.</span></span> <span data-ttu-id="360e3-127">Se um número significativo de usuários ainda não tiver sido importado no Servidor e/ou for isentado da verificação em duas etapas, deixe a caixa desmarcada.</span><span class="sxs-lookup"><span data-stu-id="360e3-127">If a significant number of users have not yet been imported into the Server and/or will be exempt from two-step verification, leave the box unchecked.</span></span>
7. <span data-ttu-id="360e3-128">Marque a caixa **Habilitar token OATH de fallback** se desejar usar senhas OATH de aplicativos móveis de verificação como um fallback para chamada telefônica, SMS ou notificação por push fora de banda.</span><span class="sxs-lookup"><span data-stu-id="360e3-128">Check the **Enable fallback OATH token** box if you want to use OATH passcodes from mobile verification apps as a fallback to the out-of-band phone call, SMS, or push notification.</span></span>
8. <span data-ttu-id="360e3-129">Clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="360e3-129">Click **OK**.</span></span>

<span data-ttu-id="360e3-130">Repita as etapas 4 à 8 para adicionar tantos clientes RADIUS quanto necessário.</span><span class="sxs-lookup"><span data-stu-id="360e3-130">Repeat steps 4 through 8 to add as many additional RADIUS clients as you need.</span></span>

## <a name="configure-your-radius-client"></a><span data-ttu-id="360e3-131">Configurar o cliente RADIUS</span><span class="sxs-lookup"><span data-stu-id="360e3-131">Configure your RADIUS client</span></span>

1. <span data-ttu-id="360e3-132">Clique na guia **Destino**.</span><span class="sxs-lookup"><span data-stu-id="360e3-132">Click the **Target** tab.</span></span>
2. <span data-ttu-id="360e3-133">Se o Servidor de MFA do Azure estiver instalado em um servidor ingressado em domínio em um ambiente do Active Directory, selecione o domínio do Windows.</span><span class="sxs-lookup"><span data-stu-id="360e3-133">If the Azure MFA Server is installed on a domain-joined server in an Active Directory environment, select Windows domain.</span></span>
3. <span data-ttu-id="360e3-134">Se os usuários devem ser autenticados em um diretório LDAP, selecione **Associação LDAP**.</span><span class="sxs-lookup"><span data-stu-id="360e3-134">If users should be authenticated against an LDAP directory, select **LDAP bind**.</span></span>

  <span data-ttu-id="360e3-135">Para usar a associação LDAP, clique no ícone de Integração de Diretórios e edite a configuração de LDAP na guia Configurações para que o servidor possa se associar ao diretório.</span><span class="sxs-lookup"><span data-stu-id="360e3-135">To use LDAP bind, click the Directory Integration icon and edit the LDAP configuration on the Settings tab so that the Server can bind to your directory.</span></span> <span data-ttu-id="360e3-136">As instruções para configurar o LDAP podem ser encontradas no [guia de configuração de Proxy LDAP](multi-factor-authentication-get-started-server-ldap.md).</span><span class="sxs-lookup"><span data-stu-id="360e3-136">Instructions for configuring LDAP can be found in the [LDAP Proxy configuration guide](multi-factor-authentication-get-started-server-ldap.md).</span></span>

4. <span data-ttu-id="360e3-137">Se os usuários forem autenticados em outro servidor RADIUS, selecione servidores RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-137">If users should be authenticated against another RADIUS server, select RADIUS server(s).</span></span>
5. <span data-ttu-id="360e3-138">Clique em **Adicionar** para configurar o servidor para o qual o Servidor de MFA do Azure fará proxy das solicitações de RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-138">Click **Add** to configure the server to which the Azure MFA Server will proxy the RADIUS requests.</span></span>
6. <span data-ttu-id="360e3-139">Na caixa de diálogo Adicionar Servidor RADIUS, digite o endereço IP do servidor RADIUS e um segredo compartilhado.</span><span class="sxs-lookup"><span data-stu-id="360e3-139">In the Add RADIUS Server dialog box, enter the IP address of the RADIUS server and a shared secret.</span></span>

  <span data-ttu-id="360e3-140">O segredo compartilhado deve ser o mesmo no Servidor de Autenticação Multifator do Azure e no servidor RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-140">The shared secret needs to be the same on both the Azure Multi-Factor Authentication Server and RADIUS server.</span></span> <span data-ttu-id="360e3-141">Altere a porta de autenticação e a porta de contabilização se forem usadas portas diferentes pelo servidor RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-141">Change the Authentication port and Accounting port if different ports are used by the RADIUS server.</span></span>

7. <span data-ttu-id="360e3-142">Clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="360e3-142">Click **OK**.</span></span>
8. <span data-ttu-id="360e3-143">Adicione o Servidor de MFA do Azure como um cliente RADIUS no outro servidor RADIUS para que ele possa processar as solicitações de acesso enviadas do Servidor de MFA do Azure.</span><span class="sxs-lookup"><span data-stu-id="360e3-143">Add the Azure MFA Server as a RADIUS client in the other RADIUS server so that it can process access requests sent to it from the Azure MFA Server.</span></span> <span data-ttu-id="360e3-144">Use o mesmo segredo compartilhado configurado no Servidor de Autenticação Multifator do Azure.</span><span class="sxs-lookup"><span data-stu-id="360e3-144">Use the same shared secret configured in the Azure Multi-Factor Authentication Server.</span></span>

<span data-ttu-id="360e3-145">Repita estas etapas para adicionar mais servidores RADIUS e configurar a ordem em que o Servidor de MFA do Azure deve chamá-los com os botões **Mover para Cima** e **Mover para Baixo**.</span><span class="sxs-lookup"><span data-stu-id="360e3-145">Repeat these steps to add more RADIUS servers and configure the order in which the Azure MFA Server should call them with the **Move Up** and **Move Down** buttons.</span></span>

<span data-ttu-id="360e3-146">Isso conclui a configuração do servidor Azure Multi-Factor Authentication.</span><span class="sxs-lookup"><span data-stu-id="360e3-146">This completes the Azure Multi-Factor Authentication Server configuration.</span></span> <span data-ttu-id="360e3-147">O servidor agora está escutando nas portas configuradas solicitações de acesso RADIUS dos clientes configurados.</span><span class="sxs-lookup"><span data-stu-id="360e3-147">The Server is now listening on the configured ports for RADIUS access requests from the configured clients.</span></span>   

## <a name="radius-client-configuration"></a><span data-ttu-id="360e3-148">Configuração do cliente RADIUS</span><span class="sxs-lookup"><span data-stu-id="360e3-148">RADIUS Client configuration</span></span>
<span data-ttu-id="360e3-149">Para configurar o cliente RADIUS, use as diretrizes:</span><span class="sxs-lookup"><span data-stu-id="360e3-149">To configure the RADIUS client, use the guidelines:</span></span>

* <span data-ttu-id="360e3-150">Configure o dispositivo/servidor para autenticar via RADIUS para o endereço IP do servidor Azure Multi-Factor Authentication, que atuará como o servidor RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-150">Configure your appliance/server to authenticate via RADIUS to the Azure Multi-Factor Authentication Server’s IP address, which will act as the RADIUS server.</span></span>
* <span data-ttu-id="360e3-151">Use o mesmo segredo compartilhado que foi configurado anteriormente.</span><span class="sxs-lookup"><span data-stu-id="360e3-151">Use the same shared secret that was configured earlier.</span></span>
* <span data-ttu-id="360e3-152">Configure o tempo limite RADIUS para 30 a 60 segundos, de forma que haja tempo para validar as credenciais do usuário, executar a verificação em duas etapas, receber a resposta e responder à solicitação de acesso RADIUS.</span><span class="sxs-lookup"><span data-stu-id="360e3-152">Configure the RADIUS timeout to 30-60 seconds so that there is time to validate the user’s credentials, perform two-step verification, receive their response, and then respond to the RADIUS access request.</span></span>
