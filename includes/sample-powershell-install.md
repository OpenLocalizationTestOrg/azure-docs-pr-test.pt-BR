Se necessário, instale o módulo do PowerShell do Azure hello usando instruções de Olá Olá [guia do PowerShell do Azure](https://docs.microsoft.com/powershell/azureps-cmdlets-docs/)e, em seguida, execute `Login-AzureRmAccount` toocreate uma conexão com o Azure. Além disso, você precisa toohave uma chave pública SSH chamada `id_rsa.pub` no diretório de .ssh de saudação do seu perfil de usuário.
